# FINE工事管理 バグ修正実施レポート（2026-07-05）

「バグ候補洗い出しレポート_2026-07-05」の全項目を修正しました。

## 修正済み一覧

### 重要度：高
- **高-1 完了操作での取消・削除データ復活** — `app/page.jsx`（完了にする／進行中に戻す）と `components/PaymentAdmin.jsx`（自動完了）の UPDATE に status フィルタを追加。完了にできるのは「進行中(active)」のみ、進行中に戻せるのは「完了(completed)」のみ。取消済み・ゴミ箱の注文書はもう復活しません。
- **高-2 F18の支払判定が税抜のまま** — `f18Dashboard.jsx` を入出金管理と同じ「税込(total_amount_inc優先)照合＋端数10円許容」に統一。入金判定にも端数許容を追加（1円ズレで永久に一部入金のままになる問題も解消）。
- **高-3 window.confirm/promptの全廃** — 共通部品 `components/AppDialog.jsx`（useConfirm / usePrompt、Promiseベース）を新設し、**17箇所のconfirmと4箇所のprompt**を全てReactモーダルに置換。対象: 請求書保存フロー・F18完了トグル・ログアウト・振り分け（予算超過確認/自社利益計上/税込入力）・新規工事登録PDF確認・ゴミ箱（復元/完全削除）・各一覧の削除・DataSync・OrderEditForm（再発行/税込入力）・help・documents・admin/items。
- **高-4 明細消失リスクの解消** — 請求書・見積書の編集保存を「旧明細IDを控える→新明細を挿入→**挿入成功を確認してから**旧明細を削除（削除失敗時は新明細を取り消し）」方式へ変更。さらに請求書保存の握りつぶし（console.warnのみ）を廃止し、編集モードの保存失敗はエラーを明示して中断、新規のローカル退避時も「サーバー未保存」を明確に警告するようにしました。

### 重要度：中
- **中-1 経営分析の売上二重計上** — `components/Dashboard.js` を工事(project_id)単位の受注額集計に変更し、F番号・発行済み・取消/削除除外にフィルタ統一。粗利・利益率も税抜ベースに統一。`app/page.jsx` 内のダッシュボード（実際に使われている方）も月別集計の二重計上を同様に修正。
- **中-2 分割請求の税抜受注額** — PaymentAdmin の taxMap を上書き→合算に変更。
- **中-3 一覧と詳細の判定不一致** — PaymentAdmin の一覧側フィルタを詳細側と同条件（cancelled除外・is_issued必須）に統一。
- **中-4 端数差額検出の非課税無視** — OrderDistributor の検出計算を保存時と同じ税区分考慮に修正。
- **中-5 税込換算の丸め不統一** — 全ページ「課税合計に10%切捨て」に統一（page.jsx の toInc、OrderHistory のフォールバック、OrderDistributor の保存税込額をPDFの合計式と完全一致に）。
- **中-6 採番の同時実行競合** — `add_unique_doc_no.sql`（UNIQUE制約）を新規作成＋OrderDistributor に重複エラー時の自動リトライ（番号取り直し）を実装。
- **中-7 ゴミ箱請求書が請求済み表示に残る** — page.jsx の請求書取得に deleted 除外を追加。
- **中-8 1000行上限の静かな欠落** — `lib/supabase.js` に fetchAllRows（ページング全件取得）を新設し、page.jsx / PaymentAdmin / Dashboard / f18 の入金・支払・請求・見積の集計取得と、請求書一覧・見積書一覧（300件固定上限も廃止）に適用。
- **中-9 orderId の Number() 変換** — 廃止（UUIDでも数値でも動作）。
- **中-10 粗利の非課税混在** — OrderEditForm が請求書/見積書の実消費税を参照して税抜換算するように（入出金管理と同方式）。取得できない場合は従来の÷1.1。
- **中-11 UTC日付** — `todayJst()` を fiscalUtils に追加し、注文日等の日付生成4箇所をJST基準に変更。

### 重要度：低
- **かな入力** — ContractorForm（会社名・住所・担当者名）、ClientForm（元請会社名・住所）に KanaInput を適用。
- **値引き・別途行の tax_type** — 見積書の保存ペイロードに明示保存（編集・新規とも）。
- **税込入力の数量>1問題** — 割り切れない場合にズレ額を警告表示。
- **RLS再発防止** — `add_missing_rls.sql` を新規作成（invoice_deposits / invoice_lines / order_items / contractors / clients / item_masters / materials / project_tasks / ocr_logs / invoice_revisions）。
- **まとめ振り分け機能** — 中断セッションの実装を静的確認。チェックボックス→まとめ振り分け→振り分け画面→副次案件merged化まで配線は完備（動作確認は下記参照）。

## ⚠️ デプロイ前に必要な作業

1. **Supabase SQL Editor で以下を実行**（コードのpushと同時か先に）:
   - `add_missing_rls.sql` — 権限エラーの再発防止
   - `add_unique_doc_no.sql` — 番号重複防止（※ファイル内の重複チェックSQLを先に実行し、重複が0件であることを確認）
   - `add_invoice_lines_tax_type.sql` — **未実行の場合のみ**（以前の検証で実行をお願いしていたもの）
2. **git push**:
   ```
   git add -A
   git commit -m "fix: バグ洗い出しレポート全項目の修正（完了操作status保護・税込照合統一・confirmモーダル化・明細保存安全化ほか）"
   git push origin main
   ```

## デプロイ後の動作確認チェックリスト

1. 工事一覧: ✓完了ボタン → モーダルなしで即完了（従来通り）／↩️で進行中に戻る
2. 振り分け取消した工事を「完了にする」→ 取消済み注文書が復活**しない**こと
3. F18: 完了⇄進行中トグルがモーダル確認付きで動くこと／支払済バッジが税込基準になっていること
4. 請求書の編集保存 → 明細が保持されること／保存失敗時にエラーが表示されること
5. 見積書の編集保存 → 同上
6. 請求書作成: 見積書紐づけ警告がモーダルで出て、「このまま保存」で保存が進むこと
7. 経営分析ダッシュボード: 複数発注に分けた工事の売上が1回だけ計上されていること
8. 工事一覧のまとめ振り分け（2件以上チェック→まとめて振り分け）が最後まで動くこと
9. 協力会社・元請の登録フォームでローマ字入力がかなに変換されること
10. ログアウトボタンがモーダル確認付きで動くこと
