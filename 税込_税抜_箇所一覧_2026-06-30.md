# FINE システム 税込／税抜 箇所一覧

- **対象**: `FINE工事管理`（Next.js）／ `fine_ocr_app`（OCR）
- **作成日**: 2026-06-30
- **目的**: 金額が「税込」か「税抜」かを、DBカラム・計算箇所・変換箇所・表示ラベル単位で棚卸し

---

## 0. システム共通の計算ルール

| 種類 | 計算式 | 使用箇所 |
|------|--------|----------|
| 消費税 | `Math.floor(課税対象額 × 0.1)`（切り捨て・10%） | 全ページ共通 |
| 税抜→税込 `toInc` | `n + Math.floor(Math.abs(n) × 0.1)`（マイナスは `n - tax`） | f18Dashboard.jsx:17、OrderDistributor.jsx:17 |
| 税込→税抜 `toEx` | `Math.round(inc ÷ 1.1)`（四捨五入） | f18Dashboard.jsx:493 |
| 課税対象 | `tax_type === '課税' または 未設定` の行のみ合算（`非課税`/`別途` は除外） | 各明細計算 |

> 注意: `toInc` は切り捨て、`toEx` は四捨五入のため、`toEx(toInc(x))` は必ずしも `x` に戻りません（端数差が出る）。f18Dashboard では「消費税端数差額」として別途記録しています。

---

## 1. DBカラムの税区分（最重要・これが全ての前提）

| テーブル.カラム | 税区分 | 補足 |
|----------------|--------|------|
| `projects.budget` | **税込** | 元請からの受注額。下流は税抜換算時 `÷1.1` |
| `projects.management_fee` | **税込** | 既に税込で保存。`toInc` を通さない（OrderDistributor.jsx:18 明記） |
| `orders.total_amount` | **税抜** | 発注小計（明細 数量×単価 の合計） |
| `orders.billing_amount` | **税込** | 元請受注額の控え（= budget 相当） |
| `order_items.unit_price` / `amount` | **税抜** | `tax_type` 列あり（課税/非課税） |
| `estimates.subtotal` | **税抜** | ※修正後は「値引き後」小計を保存 |
| `estimates.tax_amount` | 消費税額 | 課税行のみ × 0.1 |
| `estimates.total_amount` | **税込** | subtotal(値引後) + tax |
| `estimate_lines.unit_price` / `amount` | **税抜** | `tax_type` 列あり。`__DISCOUNT__`/`__BETSUDO__` 行を含む |
| `invoices.amount` | **税抜（小計）** | ※修正後は明細由来の小計を保存 |
| `invoices.tax_amount` | 消費税額 | 課税行のみ × 0.1 |
| `invoices.total_amount` | **税込** | subtotal + tax |
| `invoice_lines.unit_price` / `amount` | **税抜** | ※修正で `tax_type` 列を保存（要マイグレーション） |
| OCR `projects.budget`（OCR取込） | **税込前提** | OCR `amount` をそのまま budget へ（税込合計の想定） |

---

## 2. 計算ロジックの箇所（ファイル・行）

### 見積書 `app/estimates/create/page.jsx`
| 行 | 変数/内容 | 税区分 |
|----|-----------|--------|
| 218–221 | `subtotal`（全明細合計） | 税抜 |
| 222–225 | `discountTotal`（値引き合計） | 税抜 |
| 226 | `subtotalAfterDiscount = subtotal - discountTotal` | 税抜 |
| 228–231 | `taxableSubtotal`（課税行のみ） | 税抜 |
| 232–235 | `taxAmount = floor(max(0, taxableSubtotal - discountTotal) × 0.1)` | 消費税 |
| 236 | `totalAmount = subtotalAfterDiscount + taxAmount` | 税込 |

### 請求書 `app/invoices/create/page.jsx`
| 行 | 変数/内容 | 税区分 |
|----|-----------|--------|
| 145–147 | `taxableSubtotal`（課税行） | 税抜 |
| 148–150 | `nonTaxableSubtotal`（非課税行） | 税抜 |
| 151 | `subtotal = 課税 + 非課税` | 税抜 |
| 152 | `taxAmount = floor(taxableSubtotal × 0.1)` | 消費税 |
| 153 | `totalAmount = subtotal + taxAmount` | 税込 |
| 133–137 | `autoTaxType`（検査手数料=非課税 等） | 区分判定 |

### 注文書 `components/OrderEditForm.js`
| 行 | 変数/内容 | 税区分 |
|----|-----------|--------|
| 145 | `totalAmount`（明細 数量×単価 合計） | 税抜 |
| 146 | `taxableTotal`（課税行） | 税抜 |
| 147 | `tax = floor(taxableTotal × 0.1)` | 消費税 |
| 148 | `grandTotal = totalAmount + tax` | 税込 |
| 149 | `billingAmount`（= budget/billing_amount） | 税込 |
| 150–151 | `profit = billingAmount(税込) - totalAmount(税抜) - 管理費(税込)` | ⚠️混在 |

### 注文書PDF `components/OrderPdf.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 157–161 | `subTotal / taxableTotal / tax / total` | 税抜→消費税→税込 |
| 164 | `incUnitPrice = floor(unit_price × 1.1)`（非課税は素通し） | 税込表示用 |
| 154, 344–364 | `showTaxInclusive` で「税込のみ表示／税抜3行表示」を切替 | 表示切替 |

### 案件登録 `components/ProjectForm.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 262–264 | `taxable / tax / grandTotal` | 税抜→消費税→税込 |
| 92, 572–588 | `budget`（元請受注） / 粗利予想 = budget - subTotal | budget=税込想定 |
| 339 | `budget: Number(formData.budget) ‖ subTotal` | ⚠️未入力時は税抜小計を代入 |

### 発注振り分け `components/OrderDistributor.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 17–23 | `toInc`（税込換算・マイナス対応） | 変換 |
| 166, 206, 302, 536, 549 | 発注済み合計 = `Σ toInc(total_amount)` | 税抜→税込 |
| 252, 518 | 自社施工 `autoAmount = round(remaining(税込) ÷ 1.1)` | 税込→税抜 |
| 386, 420 | `budget / billing_amount = originalBilling` | 税込 |
| 419 | `total_amount = blockTotal` | 税抜 |
| 917 | 発注合計(税込) = `floor(totalAllocated × 1.1)` | 税抜→税込 |

### 経営ダッシュボード `components/f18Dashboard.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 484–493 | `toInc` / `toEx` 変換関数 | 変換 |
| 506 | `billingAmount = budget` | 税込 |
| 510–514 | 発注額 `total_amount`（税抜）と `toInc` 税込換算を併算 | 両方 |
| 519–521 | `profit = billingAmountEx - 各原価(税抜) - 管理費` | 税抜ベース |
| 529 | `payBalance`（支払照合） | 税込ベース |
| 753–754 | `budgetEx = toEx(budget)`、粗利 = budgetEx - 原価 | 税抜ベース |

### 月次粗利 `components/Dashboard.js`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 72–74 | `profit = billing_amount(税込) - total_amount(税抜)` | ⚠️混在 |

### 書類管理 `app/documents/page.jsx`（請求書編集）
| 行 | 内容 | 税区分 |
|----|------|--------|
| 113–115 | `subtotal / tax_amount / total_amount` を読込 | 税抜/消費税/税込 |
| 124–128 | `税抜→税込 自動計算`: `tax = floor(subtotal × 0.1)`, `total = sub + tax` | ⚠️課税/非課税の区別なし（小計全額に課税） |
| 455–457 | 保存（subtotal/tax_amount/total_amount） | 同上 |

### 入金管理 `components/PaymentAdmin.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 19, 31–33 | `SummaryCard`（税込メイン表示・税抜サブ表示） | 表示 |
| 153–155 | `calculateTotal = subTotal + floor(taxableTotal × 0.1)` | 税抜→税込 |
| 95, 285 | 入金照合は `total_amount` と budget(税込)で比較 | 税込ベース |

### 発注書発行 `app/estimates/issue-order/page.jsx`
| 行 | 内容 | 税区分 |
|----|------|--------|
| 88–89 | `total_amount: 0`、`billing_amount: est.total_amount` | 発注=税抜(初期0)／billing=税込 |

---

## 3. 税込↔税抜「変換」が起きる境界（不整合が出やすい要注意点）

| 箇所 | 変換 | 前提 |
|------|------|------|
| `invoices/create` 541 | `budget ÷ 1.1` | budget=税込 → 税抜（正） |
| `invoices/create` 544 | `order.total_amount` をそのまま | total_amount=税抜（※修正済：以前は誤って÷1.1） |
| `invoices/create` 557–558 | 見積明細合計をそのまま | estimate_lines.amount=税抜 |
| `OrderDistributor` 252/518 | `remaining(税込) ÷ 1.1` | 自社施工の税抜単価化 |
| `f18Dashboard` 全般 | `toInc`/`toEx` 多数 | budget=税込・total_amount=税抜 |
| `documents` 124–128 | `subtotal × 1.1` 相当 | 非課税を考慮しない（要注意） |
| OCR → `sync_supabase` 142 | OCR `amount` → `budget` | OCR値=税込合計の想定（税抜しか読めない帳票では誤差） |

---

## 4. 「税込／税抜」表示ラベルがある箇所（帳票・UI）

| ファイル | ラベル |
|----------|--------|
| `estimates/create` 1410–1415, 1538–1556, 1882–1938 | 「税抜金額」「消費税（10%）」「小計（税抜）」「合計（税込）」 |
| `invoices/create` 1225–1242, 1392–1403 | 「課税対象（税抜）」「非課税合計」「消費税（10%）」「合計（税込）」 |
| `OrderEditForm` 798–806 | 「発注小計（税抜）」「消費税（10%）」 |
| `OrderPdf` 349–364 | 「合計金額（税込）」「税抜金額」「消費税（10%）」 |
| `ProjectForm` 763–765, 818, 853–854 | 「小計（税抜）」「合計（税込）」「発注金額（税込）」 |
| `OrderDistributor` 912–917, 966–967 | 「発注合計（税抜）」「発注合計（税込）」「PDF: 税込/税抜」 |
| `PaymentAdmin` 253, 879 | 「発注金額(税込)」「請求金額（税込）」 |
| `invoices/list` 144 / `estimates/list` / `schedule` | 「金額（税込）」 |
| `MaterialCostInput` 218 | 「経費合計（税抜）」 |

---

## 5. 一言まとめ

- **元請側（売上）= 税込基準**: `projects.budget`、`orders.billing_amount`、`estimates/invoices.total_amount`。
- **発注側（原価）= 税抜基準**: `orders.total_amount`、各 `*_lines/order_items` の `unit_price`・`amount`。
- **管理費（management_fee）= 税込**（変換関数を通さない）。
- **粗利計算は税抜ベースが基本**だが、`OrderEditForm` と `Dashboard.js` は `billing_amount(税込) − total_amount(税抜)` で**基準が混在**しているため、利益額の定義が意図通りか要確認。
- **`documents` ページの税計算は課税/非課税を区別しない**ため、非課税明細を含む請求書を編集すると税額が過大になり得る。
