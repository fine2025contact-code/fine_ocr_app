# FINE 工事管理システム 検証レポート（金額計算・UI）

- **対象システム**: `FINE工事管理`（Next.js 14 + Supabase）／ `fine_ocr_app`（Streamlit / Python OCR）
- **検証日**: 2026-06-30
- **検証範囲**: 金額計算ロジック、税込/税抜処理、2ファイル間のデータ受け渡し、フォーマット表示、UI/状態同期
- **本レポートの位置づけ**: 調査・検証のみ。**コード修正は未実施**。

## 重要度の凡例

| 区分 | 意味 |
|------|------|
| 高 | 金額が実際にずれる／税額が誤る |
| 中 | 条件次第でずれる・データ整合性リスク |
| 低 | UX・表示・軽微 |

---

## A. 金額計算のバグ

### A-1【高】見積→請求の取込で課税区分が消える（非課税が課税になる）
- **ファイル**: `FINE工事管理/app/invoices/create/page.jsx`
- **箇所**: `loadFromEstimate` 380–387行
- **内容**: 見積明細を請求に取り込む際、通常行を `tax_type: '課税'` とハードコード。元見積が「非課税」（例: 検査手数料）でも課税扱いになり、消費税が過剰計上され合計が狂う。
- **想定原因**: `l.tax_type` を引き継がず固定値を代入している。

### A-2【高】請求明細に tax_type を保存していない → 再読込で税額が変わる
- **ファイル**: `FINE工事管理/app/invoices/create/page.jsx`
- **箇所**: 保存 `linePayload` 664–672行・725–733行／再読込 `loadInvoiceData` 457–464行
- **内容**: `invoice_lines` 挿入時に `tax_type` 列を保存していない。再読込時は `l.tax_type || '課税'` で全行が課税に戻る。非課税行・「別途」行を含む請求書を保存→開き直すと税額・合計が増える。
- **想定原因**: INSERTペイロードに `tax_type` の項目が欠落。

### A-3【中】請求書の `amount`（税抜）列が明細と同期せず、0や古い値で保存される
- **ファイル**: `FINE工事管理/app/invoices/create/page.jsx`
- **箇所**: `handleSave` payload 614行 `amount: Number(formData.amount || 0)`
- **内容**: 税抜額 `amount` 列は `formData.amount` を保存するが、明細(`invoiceLines`)を編集しても `formData.amount` は更新されない。特に見積から取り込んだ請求は `formData.amount` が空のまま → `amount=0` 保存（`tax_amount`/`total_amount` は明細由来で正しい）。`amount + tax ≠ total` の不整合データになる。
- **想定原因**: 合計系は明細から `useMemo` 再計算、`amount` だけ別フィールドのまま放置。

### A-4【中】注文書の「残金を自社利益に計上」ボタンの計算が不整合
- **ファイル**: `FINE工事管理/components/OrderEditForm.js`
- **箇所**: 660–664行
- **内容**: `rem = billingAmount - Math.round(grandTotal * 1.1 / 1.1) - managementFeeNum` の `*1.1/1.1` は無意味（実質 `grandTotal`）。さらに利益計算は `profit = billingAmount - totalAmount(税抜) - 管理費`（151行）なのに、ボタンは `grandTotal(税込)` 基準で管理費を設定するため、押下後も利益に消費税分が残る（profit=税額）。「残金=利益0」という意図とズレる。
- **想定原因**: 税抜(totalAmount)基準と税込(grandTotal)基準が混在。

### A-5【中】OCRの金額自動補正が誤読を上書き・特定金額をハードコード置換
- **ファイル**: `fine_ocr_app/parsing.py`
- **箇所**: `_num` 140–144行／`_normalize_text` 202行
- **内容**:
  - ① `_num` は数値化前に `L→1, D→0, O→0, S→5, B→8` と文字置換するため、型番・コードを誤って金額化するリスク。
  - ② `"253,500"→"258,500"`、`"253500"→"258500"` を固定置換。正当に253,500円の書類が常に258,500円へ書き換わる。
- **想定原因**: 特定帳票のOCR誤読を場当たり対応した名残。

### A-6【低】`parse_universal` の金額上書きガードのゼロ漏れ
- **ファイル**: `fine_ocr_app/parsing.py`
- **箇所**: 514–516行 `if 1000 <= amt_val <= 9000000 and result.get("amount",0) > 0:`
- **内容**: 既存値が正のときだけラベル金額で上書き。`extract_amount` が0だった場合はラベル金額があっても採用されず amount=0 のままになり得る。
- **想定原因**: マイナス値保護の条件が、ゼロ→正の補完まで塞いでいる。

---

## B. 2ファイル間／ページ間のデータ受け渡しの不整合

### B-1【高】「税抜/税込」の解釈が `total_amount` で逆転
- **該当**:
  - `FINE工事管理/components/OrderEditForm.js` 217行 `orders.total_amount = totalAmount`（税抜で保存）
  - `FINE工事管理/app/invoices/create/page.jsx` 543–544行 `baseAmount = Math.round(Number(linkedOrder.total_amount) / 1.1)`（税込前提で ÷1.1）
- **内容**: 注文書は税抜で保存しているのに、請求書側は税込とみなして1.1で割るため、注文から請求の初期額が約9%過少。`projects.budget`(税込前提) と `orders.total_amount`(税抜) で税の前提が混在。
- **想定原因**: 各テーブルの金額が税込か税抜かの定義が統一されていない。

### B-2【中】OCRの金額は「税込/税抜」が混在し得るが、下流は一律「税込」とみなす
- **該当**: `fine_ocr_app/parsing.py` `parse_sumitomo_vertical` 616–630行（税込が読めないと税抜金額・工事計を採用）→ `fine_ocr_app/sync_supabase.py` 142行で `projects.budget` に格納 → FINE側は budget を税込として `÷1.1`（`invoices/create` 542行）等で扱う。
- **内容**: 抽出元によって税抜額が budget に入ることがあり、その場合に税抜を税込扱いして再度割るため金額がずれる。
- **想定原因**: 抽出値の税区分メタ情報を持たずに単一 `amount` で受け渡している。

### B-3【中】見積の `subtotal` 保存が「値引き前(総額)」で、`subtotal + tax ≠ total`
- **ファイル**: `FINE工事管理/app/estimates/create/page.jsx`
- **箇所**: `handleSave` 756–758行（`subtotal`=値引き前、`total_amount`=値引き後+税）
- **内容**: 値引きがある見積では、DBの `subtotal` と `total_amount` の差が税額と一致しない。`subtotal+tax` で総額を再計算する処理があれば狂う。値引きは別明細(`__DISCOUNT__`)に分離保存されるため、再構成には明細合算が必須。
- **想定原因**: 画面表示用の `subtotal`(総額) をそのまま保存している。

### B-4【低】OCR送信時に `fields_display` のフォールバックが効かない（脆弱）
- **ファイル**: `fine_ocr_app/app.py`
- **箇所**: 543行 `df[EDITOR_COLUMNS]`（`fields_display` 列を含めない）→ `sync_data` 610行 `row.to_dict()` → `fine_ocr_app/sync_supabase.py` `insert_fine_row` 106行 `fd = row.get("fields_display") or {}` が常に空
- **内容**: 現状は主キー列が揃っているため動作するが、`fd.get("noX_…")` 系フォールバックは全て死んでいる。列名変更時に静かに値が欠落する設計リスク。

### B-5【中】`0 or 旧値` のfalsy判定で金額が古い値に戻る
- **該当**:
  - `fine_ocr_app/sync_supabase.py` 142–144行 `row.get("5. 代金(金額)") or fd.get("no5_amount")`
  - `FINE工事管理/app/invoices/create/page.jsx` 614行 `Number(formData.amount || 0)`
- **内容**: 金額を意図的に 0 に編集すると `0` がfalsy扱いとなり、古いOCR値や別値にフォールバックして0が反映されない。
- **想定原因**: `||`/`or` による空値判定が数値0を空とみなす。

---

## C. フォーマット表示の問題

### C-1【低】見積：画面サマリーと印刷で「小計（税抜）」の定義が不一致
- **ファイル**: `FINE工事管理/app/estimates/create/page.jsx`
- **箇所**: 画面 1394–1396行は `subtotal`(値引き前)、印刷 1897–1898行は `subtotalAfterDiscount`(値引き後)
- **内容**: 同じ「小計（税抜）」ラベルで、画面は値引き前・帳票は値引き後を表示。各々の内部計算は整合だが、突き合わせると数字が食い違って見える。

### C-2【低】¥表記の不統一
- **該当**: `formatYen`（87–90行）は `¥1,234-`（末尾ハイフン）形式、明細セルは `toLocaleString()` のみ（例 1499, 1858行）でロケール未指定・¥なし。
- **内容**: 同一帳票内で `¥…-` と素の数値が混在。`toLocaleString()`（ロケール引数なし）は実行環境により区切りが変わる可能性。

---

## D. UI／状態同期の不具合

### D-1【低】コピーモーダルの2ボタンが同一動作
- **ファイル**: `FINE工事管理/app/estimates/create/page.jsx`
- **箇所**: 1000–1007行
- **内容**: 「後で変更する」も「この内容で作成する」も `setShowCopyModal(false)` のみで挙動が同じ。確定/保留の区別がなく誤解を招く。

### D-2【中】請求の合計は明細連動だが保存 `amount` 列は非連動（A-3の表示面）
- **内容**: 画面の税抜/税込ボックス（1238/1242行）は明細から再計算され正しいが、保存される `amount` 列とズレるため、一覧・集計画面で値が食い違う可能性。

### D-3【低】OCR編集表で行追加(`num_rows="dynamic"`)した新規行は `fields_display` 無し
- **ファイル**: `fine_ocr_app/app.py`
- **箇所**: 542–557行
- **内容**: 手追加行は抽出メタを持たず、金額0や未入力で送信されると budget=0 等になる。バリデーションが金額に無い（`5. 代金` は最小値のみ）。

---

## 補足（設計上の確認ポイント／バグ断定ではない）

- **「※別途」行の合計非加算**: 見積・請求とも合計/税額に意図的に非加算（estimate 218–236行で betsudo を集計対象外、invoice では `tax_type:'別途'` が課税/非課税どちらにも入らず除外 145–151行）。仕様通りなら問題なし。利用者が「別途も合計に含む」と期待している場合は要確認。
- **注文書の利益計算の税基準混在**: `profit = billingAmount - totalAmount(税抜) - 管理費`（OrderEditForm 151行）は、`billingAmount`（budget＝税込想定）と `totalAmount`（税抜）で税基準が混在。利益額の定義が意図通りかを確認。

---

## 優先対応の推奨

| 優先 | 項目 | 理由 |
|------|------|------|
| 1 | A-1 / A-2 | 請求書の税区分喪失で税額・合計が直接誤る |
| 2 | B-1 | 注文↔請求で税込/税抜が逆転し約9%ずれる |
| 3 | A-3 / D-2 | 保存`amount`列の不整合（集計・突合に影響） |

> 修正はご指示があった項目のみ着手します。

---

## 修正対応状況（2026-06-30 実施）

| 項目 | 状態 | 修正ファイル・箇所 | 修正内容 |
|------|------|--------------------|----------|
| A-1 | ✅修正 | `invoices/create/page.jsx` `loadFromEstimate` | 通常行の `tax_type` を `l.tax_type || '課税'` に変更し元見積の課税区分を引継ぎ。検索取込側の `estimate_lines` SELECT にも `tax_type` を追加 |
| A-2 | ✅修正 | `invoices/create/page.jsx` 保存処理(編集/新規) | `invoice_lines` の INSERT ペイロードに `tax_type` を追加。**別途 `add_invoice_lines_tax_type.sql` の実行が必要** |
| A-3 / D-2 | ✅修正 | `invoices/create/page.jsx` `handleSave` | `amount` 列を `formData.amount` → 明細由来の `subtotal` に変更（`amount + tax = total` を保証） |
| A-4 | ✅修正 | `OrderEditForm.js` 残金計上ボタン | 無意味な `*1.1/1.1` を除去。利益式(税抜基準)に合わせ `管理費 = billingAmount - totalAmount` を設定し、押下後に利益が0になるよう修正。表示条件も税抜基準に統一 |
| A-5 | ✅修正 | `parsing.py` `_normalize_text` | 金額の固定置換 `"253,500"→"258,500"` / `"253500"→"258500"` を削除（正当な金額の書換え防止） |
| A-6 | ✅修正 | `parsing.py` `parse_universal` | 金額ガードを `> 0` → `>= 0` に変更し、未検出(0)時のラベル金額補完を有効化（負値=値引きの保護は維持） |
| B-1 | ✅修正 | `invoices/create/page.jsx` `handleSelectSearchResult` | `orders.total_amount`(税抜) の `÷1.1` を除去しそのまま税抜ベースで使用。`projects.budget`(税込) の換算はコメント明記のうえ維持 |
| B-3 | ✅修正 | `estimates/create/page.jsx` `handleSave` | `subtotal` 保存値を値引き前 → `subtotalAfterDiscount`（値引き後）に変更し `subtotal + tax = total` を保証 |
| B-4 | ✅修正 | `app.py` `sync_data` | 編集テーブルに含まれない `fields_display` を元データ(`fine_rows`)から index で補完 |
| B-5 | ✅修正 | `sync_supabase.py` `insert_fine_row` | `or` による falsy 判定をやめ、None/空のみフォールバック。金額0を有効値として扱う |
| C-1 | ✅修正 | `estimates/create/page.jsx` 印刷プレビュー | 帳票の小計を「値引き前小計＋値引き合計行」モデルに変更し、画面の集計表示と定義を統一 |
| C-2 | ✅修正 | `estimates/create`・`invoices/create`・`OrderEditForm.js` | 金額表示の `toLocaleString()` を全て `toLocaleString('ja-JP')` に統一 |
| D-1 | ✅修正 | `estimates/create/page.jsx` コピーモーダル | 紛らわしい「後で変更する」を「閉じる」に変更 |
| D-3 | ✅修正 | `app.py` `sync_data` | 送信前に元請名必須・金額0/NaN を弾くバリデーションを追加（マイナス＝B表値引きは許容） |
| B-2 | ⚠️設計判断要 | `parsing.py` / DB | 後述（OCR金額の税込/税抜メタ情報を持たない構造的課題のため、自動修正は見送り） |

### 追加で必要な作業（DBマイグレーション）
- **`FINE工事管理/add_invoice_lines_tax_type.sql`** を Supabase の SQL Editor で1回実行してください。`invoice_lines` に `tax_type` 列が無い状態で保存すると INSERT がエラーになります（A-2 の前提）。

### B-2 を自動修正しなかった理由
OCRの抽出値は単一の `amount` で受け渡しており「税込か税抜か」のメタ情報を持ちません。これを機械的に `×1.1` 等で補正すると、正しく税込を読めているケースまで二重補正で壊すリスクがあります。下流（FINE側）は `projects.budget` を一貫して税込として扱うため、実害が出るのは「OCRが税抜額しか読めなかったフォールバック時」に限られます。恒久対応には「OCR結果に税区分フラグを持たせる」または「常に税込合計のみ採用する」運用判断が必要なため、方針をご指示ください。

### 補足で追加対応した点
- 検索からの見積取込（`handleSelectSearchResult`）でも `estimate_lines.tax_type` を取得するよう SELECT を拡張（A-1 と同種の取りこぼし防止）。

### 検証について
- ファイル編集後、各修正箇所を再読込し構文・スコープ・JSX構造を目視確認済み。
- 自動コンパイル（`py_compile` / Lint）は当環境のサンドボックス(Linux VM)が起動不可のため未実施。**`npm run build`（FINE工事管理）と `python -m py_compile`（fine_ocr_app）をローカルで実行のうえ動作確認をお願いします。**

---

## 過去セッション照合に基づく追加修正（2026-06-30 追記）

過去のClaudeセッション履歴を確認したところ、セッション「**自社工事費と管理費の税抜処理**」で、ユーザーから明確に **「粗利は税抜にしてください」** という指示があり、当時「粗利 = 受注額(税抜) − 外注費(税抜) − 自社工事費(税抜) − 管理費」で実装する方針が確定していました。この方針に照らして以下を修正・是正しました。

| 項目 | 状態 | 修正ファイル・箇所 | 内容 |
|------|------|--------------------|------|
| A-4（再修正） | 🔁是正 | `OrderEditForm.js` 残金計上ボタン | **前回の私の修正が当時の正しいロジックを壊していた**ため是正。管理費(税込)は `billingAmount(税込) − grandTotal(税込)` で計上する元の挙動に戻した（`OrderDistributor` の残金計上と統一）。前回の `billingAmount − totalAmount(税抜)` は消費税分を二重計上していた |
| 粗利-1 | ✅追加修正 | `OrderEditForm.js` 151行付近 | `profit = billingAmount(税込) − totalAmount − 管理費` を、受注額を税抜換算した **`billingAmountEx = round(billing/1.1)` ベース**に変更（過去方針「粗利は税抜」に統一）。利益率も税抜ベースに |
| 粗利-2 | ✅追加修正 | `Dashboard.js` 72–74行 | 月次粗利グラフの `profit = 売上(税込) − 原価(税抜)` を、**`round(売上/1.1) − 原価` の税抜ベース**に変更 |

### 補足
- `management_fee`（管理費）は**税込で保存**する運用（`OrderDistributor.jsx` のコメントで明示）。粗利計算では `f18Dashboard.jsx` の既存実装に合わせ、管理費は換算せずそのまま差し引いています。
- セッション履歴で確認できた税の基本方針は「**DB保存＝税抜（unit_price等）／画面表示＝税込統一／粗利＝税抜**」。今回の A-1〜D-3 の修正および本追加修正は、いずれもこの方針と整合しています。
- 一方、`documents` ページの税計算（課税/非課税を区別せず小計全額に課税）については、過去セッションに明確な指示が見当たらなかったため**未修正のまま**としています。仕様確認のうえご指示ください。
