# FINE システム 税込／税抜 一覧（ページ別）

- **対象**: `FINE工事管理`（Next.js）／ `fine_ocr_app`（OCR）
- **作成日**: 2026-06-30
- **見方**: ページ（画面）ごとに「計算」「DB保存の税区分」「税込↔税抜の変換」「表示ラベル」「注意点」を記載

> 共通ルール … 消費税 = `floor(課税対象 × 0.1)`／`toInc`(税抜→税込)=`n+floor(|n|×0.1)`／`toEx`(税込→税抜)=`round(inc÷1.1)`／課税対象 = `tax_type==='課税' or 未設定`（非課税・別途は除外）

---

## 1. 見積書作成　`app/estimates/create/page.jsx`

- **計算**
  - 218–221 `subtotal`（全明細合計）= **税抜**
  - 222–226 `discountTotal`／`subtotalAfterDiscount = subtotal - 値引き` = **税抜**
  - 228–235 `taxableSubtotal`（課税行）→ `taxAmount = floor(max(0, taxableSubtotal - 値引き) × 0.1)` = **消費税**
  - 236 `totalAmount = subtotalAfterDiscount + taxAmount` = **税込**
- **DB保存**: `estimates.subtotal`=税抜(値引き後)／`tax_amount`=消費税／`total_amount`=税込／`estimate_lines.unit_price・amount`=税抜（`tax_type`列あり）
- **表示ラベル**: 「小計（税抜）」「値引き合計」「消費税（10%）」「税抜金額」「合計（税込）」
- **注意点**: 「※別途」行は合計・税額に**含めない**仕様。

## 2. 見積一覧　`app/estimates/list/page.jsx`

- **計算**: なし（表示のみ）
- **表示**: `total_amount` を「金額」として表示 = **税込**

## 3. 発注書発行　`app/estimates/issue-order/page.jsx`

- **DB保存**: 注文書を新規作成し `orders.total_amount = 0`（**税抜**・初期0）／`orders.billing_amount = est.total_amount`（**税込**）
- **注意点**: ここで「元請受注額(税込)＝billing_amount」「発注額(税抜)＝total_amount」の役割が確定。

## 4. 請求書作成　`app/invoices/create/page.jsx`

- **計算**
  - 145–147 `taxableSubtotal`（課税行）= **税抜**
  - 148–151 `nonTaxableSubtotal`／`subtotal = 課税 + 非課税` = **税抜**
  - 152 `taxAmount = floor(taxableSubtotal × 0.1)` = **消費税**
  - 153 `totalAmount = subtotal + taxAmount` = **税込**
  - 133–137 `autoTaxType`（検査手数料→非課税 等）
- **DB保存**: `invoices.amount`=税抜小計／`tax_amount`=消費税／`total_amount`=税込／`invoice_lines`=税抜（`tax_type`列・要マイグレーション）
- **税込↔税抜の変換**（案件転記時 `handleSelectSearchResult`）
  - 541 `budget ÷ 1.1`（budget=税込 → 税抜）
  - 544 `order.total_amount` を**そのまま**使用（=税抜。※以前は誤って÷1.1していた→修正済）
  - 557–558 見積明細合計をそのまま（estimate_lines=税抜）
- **表示ラベル**: 「課税対象（税抜）」「非課税合計」「消費税（10%）」「合計（税込）」

## 5. 請求一覧　`app/invoices/list/page.jsx`

- **計算**: なし（表示のみ）
- **表示**: 144行ヘッダ「金額（税込）」／`total_amount` を表示 = **税込**

## 6. 注文書編集　`app/orders/edit/page.jsx` ＋ `components/OrderEditForm.js`

- **計算**（OrderEditForm.js）
  - 145 `totalAmount`（数量×単価 合計）= **税抜**
  - 146–147 `taxableTotal` → `tax = floor(taxableTotal × 0.1)` = **消費税**
  - 148 `grandTotal = totalAmount + tax` = **税込**
  - 149 `billingAmount`（= budget / billing_amount）= **税込**
  - 150–151 `profit = billingAmount(税込) − totalAmount(税抜) − 管理費(税込)` … ⚠️**税基準が混在**
- **DB保存**: `orders.total_amount`=税抜／`billing_amount`=税込／`order_items.unit_price・amount`=税抜（`tax_type`列あり）／`projects.budget`=税込・`management_fee`=税込
- **表示ラベル**: 「発注小計（税抜）」「消費税（10%）」
- **注意点**: 「残金を自社利益に計上」ボタンは税抜基準(totalAmount)で管理費を設定するよう修正済。

## 7. 注文書PDF　`components/OrderPdf.jsx`

- **計算**
  - 157–161 `subTotal / taxableTotal / tax / total` = 税抜→消費税→税込
  - 164 `incUnitPrice = floor(unit_price × 1.1)`（非課税は素通し）= **税込表示用単価**
- **表示切替**: 154, 344–364 `showTaxInclusive` で「税込のみ表示」/「税抜＋消費税＋税込の3行」を切替
- **表示ラベル**: 「合計金額（税込）」「税抜金額」「消費税（10%）」

## 8. 発注振り分け　`components/OrderDistributor.jsx`

- **変換関数**: 17–23 `toInc`（税抜→税込・マイナス対応）
- **計算**
  - 166,206,302,536,549 発注済み合計 = `Σ toInc(total_amount)`（税抜→税込で集計）
  - 252,518 自社施工 `autoAmount = round(remaining(税込) ÷ 1.1)`（税込→税抜単価化）
  - 917 発注合計(税込) = `floor(totalAllocated × 1.1)`
- **DB保存**: `orders.total_amount = blockTotal`（税抜）／`billing_amount・budget = originalBilling`（税込）／`order_items`=税抜
- **注意点**: 471–498 予算(税込)と発注合計(税込換算)の差を「消費税端数差額」としてメモ記録。
- **表示ラベル**: 「発注合計（税抜）」「発注合計（税込）」「元の受注額（税込・編集可）」「PDF: 税込/税抜」

## 9. 案件登録　`components/ProjectForm.jsx`

- **計算**: 262–264 `taxable → tax → grandTotal`（税抜→消費税→税込）
- **DB保存**: 339 `budget = Number(formData.budget) ‖ subTotal` … budget=**税込想定**だが ⚠️**未入力時は税抜小計(subTotal)を代入**
- **表示**: 587 粗利予想 = `budget − subTotal`（同一基準で計算）
- **表示ラベル**: 「小計（税抜）」「合計（税込）」「発注金額（税込）」

## 10. 経営ダッシュボード　`components/f18Dashboard.jsx`

- **変換関数**: 484–493 `toInc` / `toEx`
- **計算**
  - 506 `billingAmount = budget`（税込）
  - 510–514 発注額 `total_amount`(税抜) と `toInc` 税込換算を両方算出
  - 519–521 `profit = billingAmountEx − 原価(税抜) − 管理費`（**税抜ベース**）
  - 529 `payBalance` 支払照合（**税込ベース**）
  - 753–754 `budgetEx = toEx(budget)`、粗利 = budgetEx − 原価
- **表示ラベル**: 「受注(税込)／税抜」「自社工事費(税抜)」「請求金額（税込）」など

## 11. 月次粗利グラフ　`components/Dashboard.js`

- **計算**: 72–74 `profit = billing_amount(税込) − total_amount(税抜)` … ⚠️**税基準が混在**

## 12. 入金管理　`components/PaymentAdmin.jsx`

- **計算**: 153–155 `calculateTotal = subTotal + floor(taxableTotal × 0.1)`（税抜→税込）
- **照合**: 95,285 入金照合は `total_amount` と `budget`(税込) で比較（**税込ベース**）
- **表示ラベル**: 19/31–33 税込メイン＋税抜サブ表示、「発注金額(税込)」「請求金額（税込）」

## 13. 経費入力　`components/MaterialCostInput.js`

- **表示**: 218 「経費合計（税抜）」= **税抜**

## 14. 書類管理（請求書編集）　`app/documents/page.jsx`

- **計算**: 124–128 税抜→税込 自動計算 `tax = floor(subtotal × 0.1)`／`total = subtotal + tax`
- **DB保存**: 455–457 `subtotal`(税抜)／`tax_amount`(消費税)／`total_amount`(税込)
- **注意点**: ⚠️ここの税計算は**課税/非課税を区別せず**小計全額に10%課税（非課税明細を含むと税額過大）。

## 15. スケジュール　`app/schedule/page.jsx`

- **計算**: なし（表示のみ）／`total_amount` を表示 = **税込**

## 16. OCRアプリ　`fine_ocr_app`（parsing.py / sync_supabase.py / app.py）

- **抽出**: `parsing.py extract_amount` … 「合計(税込)」「税込合計」「今回発注額」等を優先抽出 → 原則**税込**。読めない場合のフォールバックで「税抜金額」「工事計」を採用することあり。
- **手入力**: `app.py` 430 「受注額（税込）」= **税込**
- **DB保存**: `sync_supabase.py` 142 OCR `amount` → `projects.budget`（**税込前提**で格納）
- **注意点**: ⚠️抽出値が税抜だった場合でも下流は budget=税込として `÷1.1` 換算するため誤差が出る（構造的課題＝検証レポート B-2）。

---

## 早見表（税区分の結論）

| 区分 | 該当 |
|------|------|
| **税込** | `projects.budget`、`orders.billing_amount`、`estimates/invoices.total_amount`、`projects.management_fee`、各画面「合計」 |
| **税抜** | `orders.total_amount`、`*_lines/order_items.unit_price・amount`、`estimates.subtotal`、`invoices.amount`、各画面「小計」 |
| **消費税額** | `tax_amount`（課税行のみ×0.1・切り捨て） |
| ⚠️**基準混在/要確認** | `OrderEditForm`・`Dashboard.js` の粗利（税込−税抜）、`documents` の税計算（非課税無視）、`ProjectForm` の budget 既定値、OCR金額の税区分 |
