# FINE工事管理 バグ候補洗い出しレポート（2026-07-05）

過去のバグ修正セッション（完了判定の税込化、見積書編集データ消失、window.confirmブロック、S/Mバッジ、RLS権限エラー、番号重複、かな入力 ほか）と 6/30 検証レポート（A-1〜D-3）を踏まえ、**同じ原因パターンがまだ残っている箇所**を中心に再点検した結果です。コード修正は未実施です。

## 重要度：高（実データが壊れる・操作が黙って失敗する）

### 高-1. 「完了にする」が取消済み・削除済みの注文書まで復活させる
- **箇所**: `app/page.jsx` 1101–1104行（handleCompleteProject）、1116–1119行（handleRevertProject）、`components/PaymentAdmin.jsx` 317–320行（checkAndAutoComplete）
- **内容**: `UPDATE orders SET status='completed'`（または 'active'）を `project_id + F番号` だけで実行し、**status のフィルタがない**。振り分け取消で `cancelled` にした注文書や、ゴミ箱の `deleted` 注文書まで「完了」「進行中」に書き換わり復活する。自動完了（入金・支払完了時）でも同じことが起きる。
- **対処案**: `.in('status', ['active','completed'])` 等で対象を限定する。

### 高-2. F18ダッシュボードの支払判定が「税抜のまま」（過去修正の直し漏れ）
- **箇所**: `components/f18Dashboard.jsx` 95行・108–112行
- **内容**: 支払合計（**税込実額**）を `orders.total_amount`（**税抜**）と比較して「支払済」を判定。入出金管理（PaymentAdmin）は過去の修正で税込照合（total_amount_inc）＋端数許容EPS=10に統一済みだが、F18は未修正。**税抜額に達した時点で「支払済」と早く表示される**。
- **付随**: 入金判定（103行 `depositTotal >= budget`）に端数許容がなく、消費税1円ズレで永久に「一部入金」のまま。
- **対処案**: PaymentAdmin と同じ `orderInc()`（total_amount_inc優先）＋ EPS 許容に統一。

### 高-3. window.confirm が17箇所残存（「ボタンを押しても何も起きない」の再発源）
- **経緯**: 過去に工事一覧の✓完了ボタンが `window.confirm` ブロックで無反応になり、Reactモーダルに置換して解決した。同じ原因が以下に残っている。confirm がブロックされる環境では **falseが返り、処理が黙って中断される**。
- **特に危険な箇所**:
  - `app/invoices/create/page.jsx` 791行 — **請求書保存フロー内**。見積書の紐づけ警告がブロックされると保存自体が中断され、保存できたように見えて保存されない。
  - `components/f18Dashboard.jsx` 130行 — 完了⇄進行中トグル。「F18で進行中に戻せます」と案内している機能そのものが環境によって無反応になる。
  - `app/page.jsx` 945行 — ログアウト（過去に「Logout issue」セッションあり）。
  - `components/OrderDistributor.jsx` 390行 — 予算超過時の確認。ブロックされると超過案件を登録できない。
  - `components/ProjectForm.jsx` 390行 — 登録後のPDF発行確認。
- **その他**: OrderTrash（復元・完全削除）、ContractorList / ClientList（削除）、estimates/list・invoices/list（ゴミ箱）、admin/items、help、OrderEditForm（再発行）、DataSync、OrderDistributor 1101行。
- **対処案**: PaymentAdmin の showConfirm / page.jsx の confirmModal と同じReactモーダルへ順次置換。

### 高-4. 明細の「削除→再挿入」が非トランザクションで、失敗時に明細が消える
- **箇所**: `app/invoices/create/page.jsx` 735–748行、`app/estimates/create/page.jsx` 786–840行（編集モード）
- **内容**: 編集保存時に `invoice_lines` / `estimate_lines` を DELETE してから INSERT。**DELETE成功後にINSERTが失敗（RLS・ネットワーク・未実行マイグレーション等）すると明細が完全消失**する。過去の「見積書編集データ消失問題」と同型。さらに `invoices/create` 735行の DELETE と 748行の INSERT は**エラーチェックなし**。
- **付随**: `invoices/create` 864–866行で保存失敗を `console.warn` のみで握りつぶし、処理がローカル保存へ続行するため、**Supabase保存に失敗してもユーザーには成功したように見える**。
- **対処案**: Supabase RPC（Postgres関数）で削除＋挿入を1トランザクション化するか、最低限「INSERT成功を確認してから旧明細を削除」する順序に変更。エラー時は明確に画面表示。

## 重要度：中（条件次第で金額・表示がズレる）

### 中-1. 経営分析ダッシュボードの売上二重計上
- **箇所**: `components/Dashboard.js` 71–96行、`components/OrderDistributor.jsx` 438行
- **内容**: 振り分けで作る**全注文書に工事の受注額（billing_amount）を丸ごとコピー**しており、Dashboard は注文書ごとに billing_amount を合算。1つの工事を3社に分けて発注すると**受注額が3倍にカウント**される。さらに `.neq('status','deleted')` のみで **cancelled・未発行(is_issued=false)・F以外の注文書も集計対象**。
- **対処案**: project_id 単位で受注額をユニーク化して集計し、フィルタを PaymentAdmin と揃える。

### 中-2. 複数請求書がある工事の税抜受注額が誤る
- **箇所**: `components/PaymentAdmin.jsx` 197–203行
- **内容**: `taxMap[project_id] = 税額` を forEach で**上書き**するため、分割請求（1工事に複数請求書）では最後の1枚の tax_amount しか使われない。`billingAmountEx = budget − recvTax` が過大になり、粗利・利益率が狂う。
- **対処案**: 同一 project_id の tax_amount を合算する。

### 中-3. 入出金管理：一覧フィルタと詳細画面の判定条件が不一致
- **箇所**: `components/PaymentAdmin.jsx` 625–627行（一覧側） vs 546–551行（詳細側）
- **内容**: 一覧側の外注費集計は `deleted` のみ除外（**cancelled を含み、is_issued 不問**）、詳細側は cancelled 除外＋is_issued 必須。取消注文書がある工事で「一覧では未払、開くと支払済」のような食い違いが起こる。
- **対処案**: フィルタ条件を共通関数化して統一。

### 中-4. 消費税端数差額の自動検出が非課税行を無視
- **箇所**: `components/OrderDistributor.jsx` 491–493行
- **内容**: 端数差額の計算 `newAllocInc` はブロック合計に一律 `toInc()`（全額課税扱い）。一方、実際に保存する税込額（424–425行）は非課税行を除いて計算している。**非課税行を含む振り分けでは端数差額を誤検出・見逃し**する。
- **対処案**: 424行と同じ tax_type 考慮の計算に揃える。

### 中-5. 税込換算の丸め方式がページごとに不統一（1〜数円ズレ）
- **箇所**:
  - `app/page.jsx` 41行: `Math.round(v * 1.1)`（四捨五入・全額課税前提）
  - `components/PaymentAdmin.jsx` 528行: `Math.floor(abs * 0.1)`（切捨て）
  - `components/OrderDistributor.jsx` 425行 / `OrderPdf.jsx` 164行: **明細行ごと**に切捨て
  - `OrderPdf.jsx` 160行 / `f18Dashboard.jsx` 155行: **合計に対して**切捨て
- **内容**: 行ごと切捨ての合算と合計切捨てでは結果が異なる（行数分ズレ得る）。total_amount_inc（保存値）とPDF表示額が食い違うと、協力会社の請求額と照合が合わない。EPS=10 で概ね吸収されるが、明細が多いと超える可能性。
- **対処案**: 「税込＝合計課税額の10%切捨て」を共通ユーティリティに一本化し、全ページで共用。

### 中-6. 番号採番の同時実行競合（F/M番号は無対策）
- **箇所**: `lib/fiscalUtils.js` generateDocNo、`components/OrderDistributor.jsx` 408–421行、`components/ProjectForm.jsx` 306行
- **内容**: 「現在の最大＋1」方式のため、2人が同時に登録すると同番号が発行される。S番号のみ保存直前の重複チェックあり（それでも非アトミック）。`fix_duplicate_order_no.js` の存在が実発生を裏付けている。
- **対処案**: DBに UNIQUE 制約を張り、衝突時はリトライ。将来的にはDB側シーケンス採番。

### 中-7. ゴミ箱の請求書が工事一覧のS表示・請求済み判定に残る
- **箇所**: `app/page.jsx` 977行
- **内容**: 工事一覧の請求書取得が `.neq('status','cancelled')` のみで、**請求書のゴミ箱は status='deleted'**（invoices/list 79行）。削除した請求書が invoice_status / 請求額表示に反映され続ける。PaymentAdmin 側（192行）は deleted・cancelled 両方除外済みで不一致。
- **対処案**: `.neq('status','deleted')` を追加。

### 中-8. Supabase デフォルト1000行上限による静かなデータ欠落（将来リスク）
- **箇所**: `app/page.jsx` fetchProjects、`components/PaymentAdmin.jsx` fetchProjects、`components/Dashboard.js`、各 deposits/payments 取得（いずれも .limit なし）
- **内容**: Supabase は無指定だと最大1000行。データ増加時に**エラーなく集計から欠落**する（入金合計が足りない＝未回収誤表示など）。`invoices/list` は limit(300) 固定で、301件目以降が見えない。
- **対処案**: ページネーション（.range）または明示的な大きめ limit ＋件数警告。

### 中-9. orders.id の型と `Number()` 変換（要確認）
- **箇所**: `app/invoices/create/page.jsx` 861行 `.eq('id', Number(formData.orderId))`
- **内容**: orders.id が bigint なら問題ないが、UUID なら `Number()` で NaN になり更新が空振りする。過去に invoices.project_id の bigint/UUID 型不一致事故（add_project_id_uuid.sql）があったのと同型のリスク。**Supabaseで orders.id の型を確認**推奨。

### 中-10. 注文書編集画面の粗利が非課税混在で過大
- **箇所**: `components/OrderEditForm.js` 164行 `billingAmountEx = Math.round(billing / 1.1)`
- **内容**: 全額課税前提の÷1.1換算。PaymentAdmin は請求書の実消費税から税抜を算出するため、**非課税（検査手数料等）を含む工事では2画面の粗利が一致しない**。invoices/create 613行（budget÷1.1）も同様。
- **備考**: 6/30検証で「documentsページは現状維持」と判断済みの論点の周辺。運用で許容するなら現状維持でも可。

### 中-11. 日付が UTC 基準（朝9時前の登録は前日扱い）
- **箇所**: `components/OrderDistributor.jsx` 430行ほか `new Date().toISOString().split('T')[0]` を使う箇所
- **内容**: toISOString は UTC のため、日本時間 0:00〜8:59 の登録は order_date が前日になる。月初・月末の登録が月間集計（page.jsx の monthStats は order_date 基準）でひと月ズレる。
- **対処案**: JSTでの日付文字列生成ヘルパーに統一。

## 重要度：低（表示・UX・残作業）

- **低-1. かな入力の適用が未完了の可能性**: KanaInput 使用は ProjectForm / OrderEditForm / invoices/create / estimates/create の4ファイルのみ。**「全ページかな入力」対応セッションはAPIエラーで中断しており**、ContractorForm・ClientForm・PaymentAdmin のメモ欄・documents ページ等は未適用のまま。
- **低-2. まとめ振り分け機能も中断の可能性**: 「FINE楽しょるの請求書機能」セッション（ProjectList チェックボックス＋まとめ振り分け）は編集途中でAPIエラー終了。**機能が半端に実装されていないか動作確認**を推奨（チェックボックス選択→まとめ振り分け→登録→入出金合算まで一連）。
- **低-3. 税込入力の逆算が数量>1でピタリ合わない**: `OrderEditForm.js` 153行 `qty × round(ex/qty) ≠ ex`。数量1で入力する運用なら実害なし。
- **低-4. RLS未設定テーブルの再発リスク**: invoice_deposits はチャット内SQLでポリシー追加済みのはずだが、**実行済みかSupabaseで要確認**。今後テーブル追加時は「CREATE TABLE ＋ RLS ＋ POLICY」をセットで実行する運用に（過去の「権限がありません」エラーの原因）。
- **低-5. alert() 多数**: confirm と同様にブロックされる環境ではメッセージが表示されない（処理は止まらないため実害は小）。トースト表示への置換が望ましい。
- **低-6. 見積の値引き・別途行に tax_type 未設定**: `estimates/create` 807–830行の __DISCOUNT__ / __BETSUDO__ 行は tax_type を保存せずDBデフォルト任せ。再読込時の税計算前提と食い違う可能性。

## 過去指摘（6/30 A-1〜D-3）の反映状況の確認結果

- A-1（見積→請求で tax_type 消失）: 修正済みを確認（tax_type 引継ぎあり）
- A-2（invoice_lines に tax_type 未保存）: 修正済み（保存ペイロードに tax_type あり）。**ただし add_invoice_lines_tax_type.sql の実行が前提** — 未実行なら保存エラーになるため要確認
- A-5（OCR 253,500→258,500 ハードコード）: 削除済みを確認。L→1等の文字置換（parsing.py 142行）は残存（仕様として許容中）
- A-6（金額0の上書きガード）: 修正済み（`>= 0` に変更）
- B-1（total_amount の税込/税抜逆転）: 修正済み（÷1.1 廃止のコメントあり）
- B-5（0 or 旧値のfalsy判定）: 修正済み（is not None 判定に変更）
- B-2（OCR金額の税込/税抜判別）・documentsページ税計算: ユーザー判断で「対応しない」確定済み

## 推奨対応順

1. 高-1（完了操作での取消・削除データ復活）— データ破壊系で最優先
2. 高-4（明細 delete→insert の消失リスク）— 実際に一度発生した問題の残存形
3. 高-2（F18の税抜払い判定）— 過去修正の直し漏れ
4. 高-3（window.confirm 置換）— 特に請求書保存フロー791行から
5. 中-1・中-2（ダッシュボード・粗利の金額精度）
6. 残りの中・低項目
