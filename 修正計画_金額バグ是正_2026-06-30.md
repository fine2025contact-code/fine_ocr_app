# FINE 金額バグ是正 修正計画（最初からの手順書）

- 対象: `FINE工事管理`（Next.js）／ `fine_ocr_app`（OCR）／ Supabase
- 作成: 2026-06-30
- 目的: 税込/税抜の取り違え・丸め端数・消込判定の誤りを根絶し、入出金管理を信頼できる状態にする
- 原則: **お金に直結するため「仕様合意 → 土台 → 確実な是正 → 端数根絶 → 検証」の順。各段階で確認をはさみ、いつでも戻せる状態を保つ。**

---

## 基本となる税ルール（過去方針で確定済み・本計画の前提）

| 区分 | 内容 |
|------|------|
| DB保存（明細・単価・発注） | **税抜**（`unit_price` / `order.total_amount` / `*_lines.amount`） |
| 受注・合計 | **税込**（`projects.budget` / `orders.billing_amount` / `estimates・invoices.total_amount`） |
| 管理費 | **税込**で保存（`management_fee`） |
| 画面表示 | **税込で統一**（税抜は補助表示） |
| 粗利 | **税抜ベース** |
| 消費税 | `floor(課税対象 × 0.1)`（課税行のみ） |

> この表が「正」。全修正はこの表に一致させることをゴールとする。

---

## フェーズ0：準備・安全網（コード変更なし）

1. **Gitでバックアップ**: `git switch -c fix/money-reconcile` で作業ブランチを作成し、現状を `git commit`。
2. **Supabaseのバックアップ**: ダッシュボードでDBスナップショット/エクスポート（特に `projects` `orders` `invoices` `estimates` `*_deposits` `order_payments`）。
3. **テスト用の検証ケースを用意**: 数字が分かっている案件を3〜5件選び、「受注額・外注費・粗利・入金・支払・残」の正解値を控える（修正前後で突合するため）。
4. **確認環境**: 可能なら本番とは別のSupabaseプロジェクト or ステージングで先に試す。

リスク: なし／所要: 小

---

## フェーズ1：仕様の最終合意（要・ご判断）

コードを触る前に、現在「要判断」の項目の方針を確定する。ここが未確定だと手戻りになる。

| 決める項目 | 選択肢 | 推奨 |
|-----------|--------|------|
| 入金照合の基準 | budget（現状）／請求書発行済なら `invoice.total_amount` | まず警告表示、将来は請求額基準 |
| 管理費の粗利扱い | ラベルを「税込」に直す（最小）／粗利で `toEx` 換算（2ファイル同時） | ラベル修正（最小） |
| `documents` の税計算 | 課税/非課税を区別する／現状維持 | 区別する（過大課税の是正） |
| `ProjectForm` budget未入力時 | 税抜小計を代入（現状）／税込換算して代入／空のまま | 税込換算 or 必須化 |
| OCR金額の税区分(B-2) | 常に税込合計のみ採用／税区分フラグを持たせる | 税込合計のみ採用（運用ルール化） |
| budget_ex 保存（往復解消） | 採用（カラム追加）／許容差のみで対応 | 採用（根絶したい場合） |

リスク: なし（合意のみ）／所要: 小〜中

---

## フェーズ2：土台 — 税計算の共通化（リスク低）

**目的**: `toInc/toEx` が3ファイルに重複し定義ズレの温床。1本に集約。

- 手順
  1. `f18Dashboard.jsx`・`OrderDistributor.jsx`・`PaymentAdmin.jsx` の `toInc/toEx` 定義を**差分確認**（完全一致か）。
  2. `lib/tax.js` を新規作成:
     ```js
     export const TAX_RATE = 0.1;
     export const EPS = 10; // 円: 税端数の許容差
     export const toInc = (ex) => { const n = Number(ex)||0; return n + (n<0?-1:1)*Math.floor(Math.abs(n)*TAX_RATE); };
     export const toEx  = (inc) => Math.round((Number(inc)||0)/(1+TAX_RATE));
     export const settled = (a,b) => Math.abs((Number(a)||0)-(Number(b)||0)) <= EPS;
     ```
  3. 各ファイルの重複定義を削除し `import { toInc, toEx, EPS, settled } from '@/lib/tax'` に置換。
- リスク: 低（数式は不変）。差分確認を怠ると一部画面の数字が変わる。
- 検証: 各画面で修正前後の表示金額が一致することを確認。
- ロールバック: ブランチ破棄。

---

## フェーズ3：確定済み修正の仕上げ＋マイグレーション（リスク低〜中）

すでに適用済みの修正を本番で動く状態にする。

1. **DBマイグレーション実行**: `add_invoice_lines_tax_type.sql` を Supabase で実行（A-2の前提。未実行だと請求保存がエラー）。
2. 既適用分（A-1〜A-6 / B-1 / B-3 / B-5 / C-1 / C-2 / D-1 / D-3 / Dashboard粗利税抜）を `npm run build` と `python -m py_compile` で構文確認。
3. 検証ケースで請求書の課税/非課税が保存→再読込で維持されることを確認。
- リスク: マイグレーションはDB変更だが追加カラム（デフォルト'課税'）で後方互換。
- ロールバック: 列削除 `ALTER TABLE invoice_lines DROP COLUMN tax_type;`

---

## フェーズ4：入出金管理の確実な是正（リスク中）

**目的**: 「未払いでも工事完了」「永久に残が消えない」を解消。

1. **自動完了の支払判定を税込へ**（`checkAndAutoComplete` 277/286）: `outsourceCost`(税抜) → `outsourceCostInc`(toInc)。
2. **完了・残判定に許容差**（285/286、フィルタ576–579）: `>=` を `>= 値 - EPS` / `settled()` に。
3. **残額バッジの色判定**（1334/1379）: `!==0` → `Math.abs(x) > EPS`。
4. **統合伝票の突合をID参照に**（1130）: `mergedRefs.find(budget===amount)` をやめ、`mergedRefs` の `id` で入金を紐付け。
- リスク: 中。EPSは本当の少額不足を完了扱いにし得る → 10円程度に固定。自動完了はDBに `completed` を書くため、誤完了時に戻せるようにしておく。
- 検証: 検証ケースで「税込で残ゼロのときだけ完了」「同額の統合伝票で入金が正しい伝票に入る」ことを確認。

---

## フェーズ5：端数の根絶（リスク中〜高・中期）

**目的**: 税込↔税抜の往復で出る1〜数円差を構造的になくす。

1. **集計ルールの固定**:
   - 協力会社支払＝注文書単位で課税（`Σ toInc(注文書)`）のまま、実支払との突合は `settled()`。
   - 粗利などまとめて課税する画面は「税抜で合算 → 一度だけ課税」に統一。
2. **budget_ex の二基準保存**（フェーズ1で採用の場合）:
   - `projects` に `budget_ex`（税抜）カラム追加（マイグレーション）。
   - 既存行は `round(budget/1.1)` でバックフィル（過去分は近似、新規分は入力時に確定）。
   - OCR取込・手入力・案件登録の保存処理で `budget` と `budget_ex` を同時に保存。
   - `÷1.1` で税抜を作り直している箇所（`invoices/create` 542 など）を**全文検索で洗い出し**、`budget_ex` 読み出しに置換。
- リスク: 高。DB変更＋既存全案件の表示が数円動く可能性。読み替え漏れが不整合を生む。
- 検証: `grep "/ 1.1"` `"÷1.1相当"` の全箇所が置換済みか確認。検証ケースで往復ゼロを確認。
- ロールバック: カラムは残し、読み出しを旧 `÷1.1` に戻すだけで復帰可能（追加方式のため安全）。

---

## フェーズ6：要判断項目の反映（フェーズ1の合意に従う）

- `documents` ページ: 税計算を課税/非課税区分に対応（非課税明細の過大課税を是正）。
- `ProjectForm`: budget未入力時の代入を合意した方式に。
- 入金照合: 請求書発行済み案件で budget と請求額が食い違う場合の警告 or 基準切替。
- 管理費: ラベル/粗利の扱いを合意どおりに（`f18Dashboard` と同時）。
- OCR(B-2): 「税込合計のみ採用」の運用ルール化（or 税区分フラグ）。
- リスク: 項目により中。いずれも合意済み前提で実施。

---

## フェーズ7：検証・回帰・本番反映

1. **ビルド/構文**: `npm run build`（FINE） / `python -m py_compile *.py`（OCR）。
2. **数値回帰**: 検証ケース3〜5件で「受注・外注・粗利・入金・支払・残・完了判定」を修正前後で突合。
3. **画面横断確認**: 見積/請求/注文/振り分け/ダッシュボード/入出金で同じ案件の金額が一致するか。
4. **段階反映**: ステージング → 少数案件で試運用 → 全体。
5. **本番マイグレーション**: バックアップ後に実行。
- リスク: 検証不足が最大のリスク。お金まわりは実データ確認必須。

---

## まとめ：実施順と難易度

| フェーズ | 内容 | リスク | 効果 |
|----------|------|--------|------|
| 0 | バックアップ・テスト準備 | なし | 安全網 |
| 1 | 仕様の最終合意 | なし | 手戻り防止 |
| 2 | 税計算の共通化 | 低 | 土台 |
| 3 | 既修正の仕上げ＋マイグレーション | 低〜中 | 既知バグ確定 |
| 4 | 入出金管理の確実な是正 | 中 | **誤完了・永久残の解消（最重要）** |
| 5 | 端数の根絶（集計統一・budget_ex） | 中〜高 | 構造的解消 |
| 6 | 要判断項目 | 中 | 仕様整合 |
| 7 | 検証・回帰・反映 | — | 品質保証 |

**最小で効くのはフェーズ2〜4**（共通化＋入出金の是正）。ここまでで「払っていないのに完了」「残が消えない」は解消します。フェーズ5は端数を根絶したい場合の本丸で、DB移行を伴うため段階適用と検証を厚めに。

> 次アクション: フェーズ0（Gitコミット）を実施し、フェーズ1の「要判断6項目」を決めてください。決定後、フェーズ2→3→4の順に着手します。
